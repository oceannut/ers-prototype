'use strict'

define([ 'angular'], 
    function(angular) {

	angular.module('ers.contact.controllers', 
			[])
			.controller(
					'ContactCtrl',
					[ '$scope',
					function($scope) {

						$scope.init = function() {
							
						}
						
						

					} ]);

})
